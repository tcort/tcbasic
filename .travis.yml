language: c
complier:
  - clang
  - gcc

addons:
  apt:
    packages:
    - clang
    - cppcheck

before_script:
  - ./autogen

script:
  - scan-build ./configure
  - scan-build make
  - scan-build make check
  - cat ./test-suite.log
  - scan-build make distcheck

sudo: false
